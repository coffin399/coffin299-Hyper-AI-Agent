name: Build & Release Electron App

on:
  push:
    branches:
      - main

jobs:
  build-and-release:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Electron dependencies
        run: npm install

      - name: Install Frontend dependencies
        working-directory: ./frontend
        run: npm install

      - name: Generate lock file
        working-directory: ./frontend
        run: npm ci --production=false

      - name: Build Frontend
        working-directory: ./frontend
        run: npm run build

      - name: Copy frontend build to dist
        run: |
          mkdir -p dist
          cp -r frontend/build/* dist/

      - name: Build Electron app (Ubuntu)
        if: matrix.os == 'ubuntu-latest'
        run: npm run build -- --linux
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Electron app (Windows)
        if: matrix.os == 'windows-latest'
        run: npm run build -- --win
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Electron app (macOS)
        if: matrix.os == 'macos-latest'
        run: npm run build -- --mac
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Get current date (version)
        id: date
        run: echo "VERSION=$(date +'%Y.%m.%d')" >> $GITHUB_OUTPUT

      - name: Create or Update Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.date.outputs.VERSION }}
          name: Release ${{ steps.date.outputs.VERSION }}
          body: |
            # Hyper AI Agent v${{ steps.date.outputs.VERSION }}

            🚀 BYOK multi-provider AI agent with RAG and automation

            ## 🎯 主な機能

            ### 🤖 AIプロバイダー対応
            - OpenAI (GPT-4, GPT-3.5)
            - Anthropic (Claude-3)
            - Google Gemini
            - Ollama (ローカルモデル)
            - 自動フェイルオーバーと負荷分散

            ### 🧠 コンテキスト管理
            - 会話履歴の永続化
            - ベクトルメモリ検索 (RAG)
            - 自動要約機能
            - セマンティック検索

            ### 🛠️ ツール統合
            - ファイルシステム操作
            - Webスクレイピング
            - カレンダー管理
            - メール送信
            - コード実行 (サンドボックス)
            - データベースクエリ

            ### ⚡ 自動化
            - Cronスケジュール
            - ファイル監視トリガー
            - Webhook対応
            - ワークフロー実行

            ### 🖥️ ローカルモデル管理
            - PCスペック別モデル推奨
            - Qwen3/Gemma3対応
            - 一括ダウンロード/削除
            - リアルタイム進捗表示

            ## 📦 インストール

            ### Windows
            1. `Hyper-AI-Agent-Setup-${{ steps.date.outputs.VERSION }}.exe` をダウンロード
            2. インストーラーを実行
            3. アプリケーションを起動

            ### macOS
            1. `Hyper-AI-Agent-${{ steps.date.outputs.VERSION }}.dmg` をダウンロード
            2. DMGファイルを開く
            3. アプリケーションフォルダにドラッグ

            ### Linux
            1. `Hyper-AI-Agent-${{ steps.date.outputs.VERSION }}.AppImage` をダウンロード
            2. 実行権限を付与: `chmod +x *.AppImage`
            3. 実行: `./Hyper-AI-Agent-${{ steps.date.outputs.VERSION }}.AppImage`

            ## ⚙️ 初期設定

            1. APIキーを設定 (OpenAI, Anthropic, Google)
            2. Ollamaをインストール (ローカルモデル使用の場合)
            3. プロジェクトを作成して開始

            ## 🔗 リンク

            - ドキュメント: [README](https://github.com/coffin299/coffin299-Hyper-AI-Agent/blob/main/README.md)
            - バグ報告: [Issues](https://github.com/coffin299/coffin299-Hyper-AI-Agent/issues)
            - 機能要望: [Discussions](https://github.com/coffin299/coffin299-Hyper-AI-Agent/discussions)

            ---

            🔒 セキュリティ: APIキーはローカルでAES暗号化保存
            🌈 オープンソース: MITライセンス
          files: |
            release/*.exe
            release/*.dmg
            release/*.AppImage
          draft: false
          prerelease: false
          overwrite: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
